@extends('layouts.app')

@section('content')
    <h1>{{ $movie->title }}</h1>
    <p>ID: {{ $movie->id }}</p>
    <p>公開年: {{ $movie->published_year }}</p>
    <p>概要: {{ $movie->description }}</p>
    <p>上映中: {{ $movie->is_showing ? 'はい' : 'いいえ' }}</p>
    <p>ジャンル: {{ $movie->genre->name ?? 'N/A' }}</p>
    <p>作成日時: {{ $movie->created_at }}</p>
    <p>更新日時: {{ $movie->updated_at }}</p>
    <a href="{{ route('admin.movies.edit', $movie->id) }}">編集</a>
    <a href="{{ route('admin.schedules.create', $movie->id) }}">スケジュール作成</a>
    <h2>スケジュール</h2>
    <ul>
        @foreach ($movie->schedules as $schedule)
            <li>
                <a href="{{ route('admin.schedules.show', $schedule->id) }}">
                    開始: {{ $schedule->start_time }} - 終了: {{ $schedule->end_time }}
                </a>
            </li>
        @endforeach
    </ul>
@endsection